<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<table>
  <thead>
  <tr>
    <th>Latest Post</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
    <tr>
      <td><%=	 @user.posts.first.content %></td>
      <td><%= link_to 'Show', @user.posts.first %></td>
      <td><%= link_to 'Edit', edit_post_path(@user.posts.first) %></td>
      <td><%= link_to 'Destroy', @user.posts.first, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  </tbody>
</table>

<br>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
